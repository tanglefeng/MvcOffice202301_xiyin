@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@*@{
    @foreach (var item in Model.bb.Split(';'))
    {
        //<img src="@Url.Content(String.Format("{0}" ,Model.bb))"asp-append-version="true" />
        <img src="@Url.Content(String.Format("{0}" ,item))"asp-append-version="true" />
    }
}*@
@*using IEnumerable<MvcOffice.Models.PictureSum>*@
@model IEnumerable<MvcOffice.Models.KengicAspnetcorePictureSum>
 @foreach (var item in   ViewBag.SearchString.Split(';'))
    {
        //<img src="@Url.Content(String.Format("{0}" ,Model.bb))"asp-append-version="true" />
       // <img src="@Url.Content(String.Format("{0}" ,item))"asp-append-version="true" />
    <img src="@Url.Content("~/css/"+String.Format("{0}" +".jpg",item))" asp-append-version="true" />
    }

  @*  string aa=ViewBag.SearchString
    @*<p> <a href=+aa+>百度一下</a>.</p>*@
